void main() {
  String nama = " Ardi Hidayat ";
  String animals = "Kucing, Burung, Singa";
  var angka = 17;

//cari kata yg sama
  // print(nama.contains('Ardi'));

  // print(nama.toLowerCase());
  // print(nama.toUpperCase());

  // print(angka.toString());

  // print(animals.split(', ')[1]);
  // print(nama.substring(0, 4));

  // print(nama);
  // print(nama.trim());
  // print(nama.trimLeft());
  // print(nama.trimRight());
  // print(nama.codeUnitAt(1));
  // print(nama.indexOf('i'));
  // print(nama.startsWith(' ardi'));
  // print(nama.endsWith('Hidayat '));

  var kosong = '';
  print(kosong.isEmpty);
  print(kosong.isNotEmpty);
}
